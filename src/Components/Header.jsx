//imports
import React from 'react';
import { Link } from 'react-router-dom';
import NavbarPage from '../../src/Components/Navbar';

//header function that renders the navbar
export default function Header() {
    return (
        <header >
            < NavbarPage />
        </header>
      )
}